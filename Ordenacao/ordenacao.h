#ifndef ORDENACAO_H
#define	ORDENACAO_H

#include <stdio.h>
#include <stdlib.h>

int bubblesort (int*, int);
int shakesort (int*, int);
int insertionsort (int*, int);
int shellsort (int*, int);
int selectionsort (int*, int);
int ranksort (int*, int);
int quicksortPrimeiro (int*, int, int);
int quicksortCentral (int*, int, int);
int quicksortAleatorio (int*, int, int);
int quicksortMediana (int*, int, int);
int mergesort (int*, int, int);
int heapsort (int*, int);
int radixsort(int*,int);
int radixsortBin(int*,int);
#endif	/* ORDENACAO_H */

